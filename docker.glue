FROM mambaorg/micromamba
RUN micromamba install -y -n base -c conda-forge -c bioconda \
    jupyterlab ipython plotly scanpy pandas scipy numpy \
    faiss-gpu pytorch-gpu plotly scglue && \
    micromamba clean -y -a

# ---------- Entrypoint ----------
WORKDIR /Works
CMD ["/bin/bash"]
